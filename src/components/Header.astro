---
import ThemeToggle from './ThemeToggle.astro';
import { url } from '../lib/paths';
---

<!-- Sentinel: dopóki jest widoczny, pasek stoi na samej górze strony.
     Tańsze niż nasłuchiwanie scrolla - obserwator odpala się tylko na
     przecięciu, nie przy każdym pikselu przewinięcia. -->
<div id="header-sentinel" aria-hidden="true"></div>

<header class="site-header sticky top-0 z-50">
  <div class="mx-auto flex max-w-[1120px] items-center justify-between gap-2 px-4 py-5 text-sm sm:gap-4 sm:px-6 md:px-14">
    <a href={url('/')} class="whitespace-nowrap font-medium text-[var(--color-text)] no-underline transition-colors hover:text-[var(--color-accent-300)]">
      Michał Minor
    </a>
    <nav class="flex items-center gap-2 text-[var(--color-neutral-400)] sm:gap-4 md:gap-6">
      <!-- Projekty i Kontakt zostają widoczne na każdej szerokości - O mnie
           chowa się pierwsze, bo Projekty i tak prowadzi tam gdzie ono. -->
      <a href={url('/#projekty')} class="no-underline text-[var(--color-accent-300)] transition-colors hover:text-[var(--color-accent-200)]">Projekty</a>
      <a href={url('/#o-mnie')} class="hidden no-underline text-[var(--color-accent-300)] transition-colors hover:text-[var(--color-accent-200)] sm:inline">O mnie</a>
      <a href="mailto:minor.michal1@gmail.com" target="_blank" rel="noopener" class="no-underline text-[var(--color-accent-300)] transition-colors hover:text-[var(--color-accent-200)]">Kontakt</a>
      <ThemeToggle />
    </nav>
  </div>
</header>

<script>
  const sentinel = document.getElementById('header-sentinel');
  const header = document.querySelector('.site-header');
  if (sentinel && header) {
    new IntersectionObserver(
      ([entry]) => header.classList.toggle('is-stuck', !entry.isIntersecting),
      { threshold: 0 }
    ).observe(sentinel);
  }
</script>
